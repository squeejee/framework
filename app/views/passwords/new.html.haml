= render :partial => "shared/notice_invite_required"

#page_header
  %h2
    Request a password reset

#page_body
  = error_messages_for :user
  - form_for resource_name, resource, :url => password_path(resource_name) do |f|
    %fieldset
      = f.text_field :email
      = f.submit "Reset Password"